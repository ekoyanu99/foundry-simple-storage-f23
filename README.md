For the moment, a `$PRIVATE_KEY` in my `.env` file is just for test. Keep en eye about `keystore`, still waiting from `Foundry` about update `keystore` method.

## Documentation

https://book.getfoundry.sh/
